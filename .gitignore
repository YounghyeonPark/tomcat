# Build artifacts
/build/
/firmware/build/
*.o
*.elf
*.hex
*.bin

# CAD mesh export (large, regenerated from tomcat_skeleton.py; STEP is tracked)
mechanical/cad/*.stl

# KiCad backups / autosave
*-backups/
*.kicad_sch-bak
*.kicad_pcb-bak
_autosave-*
fp-info-cache

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editors / OS
.vscode/
.idea/
*~
.DS_Store
Thumbs.db

# Development reference image with an unestablished licence -- NOTICE records that
# this is deliberately untracked; ignoring it makes that enforceable, not just stated.
Felis_silvestris_restoration_&_skeleton.jpg

# MuJoCo writes this next to the working directory on every run.
MUJOCO_LOG.TXT
